{
  "reasoning_effort": {
    "description": "Codex 推理深度",
    "type": "string",
    "hint": "控制模型思考深度，也可用 /codex_reasoning 指令调整。级别越高越聪明但越慢、额度消耗越大。",
    "options": ["minimal", "low", "medium", "high", "xhigh"],
    "labels": ["最少（最快）", "低", "中（默认）", "高", "最高（最慢）"],
    "default": "medium"
  },
  "fast_mode": {
    "description": "Codex 1.5 倍速模式",
    "type": "bool",
    "hint": "开启后以 priority 服务层级请求，响应速度更快（约 1.5 倍），但订阅额度消耗更快。也可用 /codex_fast 指令开关。",
    "default": false
  },
  "enable_search_tool": {
    "description": "启用 Codex 联网搜索工具",
    "type": "bool",
    "hint": "注册 codex_web_search LLM 工具，让模型可调用 Codex 订阅自带的联网搜索。",
    "default": true
  },
  "force_codex_web_search": {
    "description": "强制 Codex 联网搜索",
    "type": "bool",
    "hint": "仅在 Codex 联网搜索工具已启用时生效；开启后禁用 AstrBot 自带联网搜索，关闭、禁用工具或卸载插件时自动恢复。",
    "default": false
  },
  "search_mode": {
    "description": "搜索模式",
    "type": "string",
    "hint": "live 实时抓取网页（最新最全，稍慢）；indexed 仅搜索索引；cached 仅用缓存（最快但可能过时）。",
    "options": ["live", "indexed", "cached"],
    "labels": ["实时联网（推荐）", "仅索引", "仅缓存"],
    "default": "live"
  },
  "search_context_size": {
    "description": "搜索上下文大小",
    "type": "string",
    "hint": "传给模型的搜索结果规模，越大越全面但越慢、额度消耗越多。",
    "options": ["low", "medium", "high"],
    "labels": ["小", "中（默认）", "大"],
    "default": "medium"
  },
  "image_model": {
    "description": "图片生成与编辑模型",
    "type": "string",
    "hint": "默认 auto：在线读取 OpenAI 官方图片模型目录，按数字版本选最新，同版本遵循官方排列顺序；缓存 6 小时。也可手动填 gpt-image-2.5-sunburst、gpt-image-2.5-flare 或其他 gpt-image- 模型 ID。指令 /codex_image_model 可查看、切换和刷新；手动选择固定模型，不自动替换。",
    "default": "auto"
  },
  "image_quality": {
    "description": "图片生成质量",
    "type": "string",
    "hint": "图片模型的输出质量档位。auto 由后端自动选择；改图追求画风一致性时建议锁定 high（额度消耗更大）。与 image_model 的自动选模独立。",
    "options": ["auto", "high", "medium", "low"],
    "labels": ["自动（默认）", "高", "中", "低"],
    "default": "auto"
  }
}
